<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Forgot Password - EmpowerFund</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
  <link rel="stylesheet" href="/css/main.css">
</head>
<body class="login-page-split">

  <div class="login-container">
    <div class="login-welcome-panel">
      <h2>Forgot Your Password?</h2>
      <p>No problem. Enter your email address below and we'll send you a link to reset it.</p>
    </div>

    <div class="login-form-panel">
      <h3>Reset Password</h3>
      
      <% if (messages.error && messages.error.length > 0) { %>
       <div class="alert alert-error"><%= messages.error %></div>
      <% } %>
      <% if (messages.success && messages.success.length > 0) { %>
        <div class="alert alert-success"><%= messages.success %></div>
       <% } %>

      <form action="/api/users/forgot-password" method="POST">
        <div class="form-group icon-group">
          <i class="fa-solid fa-envelope form-icon"></i>
          <input type="email" id="email" name="email" placeholder="Enter your email" required>
        </div>
        
        <button type="submit" class="btn btn-login">Send Reset Link</button>
        <div style="text-align: center; margin-top: 1.5rem;">
          <p style="margin: 0; color: #6c757d;">Remembered your password? <a href="/" style="color: #007991; text-decoration: none; font-weight: 600;">Login</a></p>
        </div>
      </form>
    </div>
  </div>

</body>
</html>